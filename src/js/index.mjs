import $ from 'jquery'
import css from '../css/template.scss'
import milligram from 'milligram'

export default function () {
  let one, two, three
  one = 1
  two = 2
  three = 3
  if (one + two === three) {
    console.log('True')
  }
}
